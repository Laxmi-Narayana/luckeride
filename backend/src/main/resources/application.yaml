spring:
  application:
    name: luckeride

  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:luckeride}
    username: ${POSTGRES_USER:luckeride}
    password: ${POSTGRES_PASSWORD:luckeride}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true